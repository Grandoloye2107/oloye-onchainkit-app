// NFT grid display
export default function NFTGrid() {
  return <div className="grid grid-cols-3 gap-4">NFTs here</div>;
}
// loading state` comment
// responsive: 2 cols mobile, 3 tablet, 4 desktop` 
// rate limiting handled with retry logic